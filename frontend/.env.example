# Relative URL only — resolves against the host the user opened (IP or domain).
# Do not use http://localhost/... here; remote clients would call their own machine.
#
# Production (IIS): /ams_be/api/
# Local Vite:       /api/
VITE_API_BASE_URL=/ams_be/api/
